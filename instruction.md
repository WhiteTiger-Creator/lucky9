Compute the stabilized configuration of the Meridian SE-draining chip-firing model. The Meridian model is a directed abelian sandpile on a finite grid — a discrete dynamical system studied for its firing dynamics and conservation properties — and `/app/docs/model_spec.md` defines its firing rule precisely. Given the chip drops in `/app/data/drops.json`, simulate the model to its unique stable configuration and report the results.

The program at `/app/stabilize.py` currently evaluates the classical four-neighbour sandpile rule, which is a different dynamical system and produces the wrong stabilized grid, firing counts, and spill. Implement the Meridian dynamics in `/app/stabilize.py` so that it stabilizes the grid under that model and writes `result.json` to the output directory with every field the model requires — the stabilized grid, `total_firings`, `row_firings`, `spill`, and the `grid_checksum` computed with the exact serialization the spec describes. Keep the `--input` and `--output-dir` flags and their defaults.

Run it when you are done so `/app/output/result.json` is in place. Derive everything from the input; don't read or import anything from `/tests` or `/solution`.
